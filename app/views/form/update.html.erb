<%= @book.title = if defined? params[:title] then params[:title] else @book.title end %>
<%= @book.author = if defined? params[:author] then params[:author] else @book.author end %>
<%= @book.rating = if defined? params[:rating] then params[:rating] else @book.rating end %>
<%= @book.save %>

<meta http-equiv="Refresh" content="0; url=/book/list">
